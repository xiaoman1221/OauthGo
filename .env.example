# 服务端口
PORT=8080

# 站点地址（前端地址，用于第三方登录回调跳转），如 http://localhost:3000
HOST=http://localhost:3000

# Gin 模式：debug / release
GIN_MODE=debug

# 数据库文件路径
DB_PATH=data.db

# JWT 签名密钥（留空则首次启动自动生成随机密钥并持久化到数据库；跨实例部署请显式配置）
JWT_KEY=
