# 服务端口
PORT=8080

# 站点地址（前端地址，用于第三方登录回调跳转），如 http://localhost:3000
HOST=http://localhost:3000

# Gin 模式：debug / release
GIN_MODE=debug

# 数据库文件路径
DB_PATH=data.db

# JWT 签名密钥（请修改为随机字符串）
JWT_KEY=change-me-to-a-random-secret
